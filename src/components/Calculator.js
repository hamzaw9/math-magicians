import CalculatorButton from './CalculatorButton';
import './style.css';

const Calculator = () => (
  <section className="calculator">
    <CalculatorButton />
  </section>
);

export default Calculator;
